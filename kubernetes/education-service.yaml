apiVersion: v1
kind: Service
metadata:
  name: education-service
  namespace: argocd
spec:
  selector:
    app: education-service
  ports:
    - protocol: TCP
      port: 9002      # 외부/내부에서 접속할 포트
      targetPort: 9002 # 컨테이너 내부 포트
  type: ClusterIP
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: education-service
#   namespace: ctrl
# spec:
#   selector:
#     app: education-service
#   ports:
#     - protocol: TCP
#       port: 9002      # 외부/내부에서 접속할 포트
#       targetPort: 9002 # 컨테이너 내부 포트
#   type: ClusterIP
