# 📘 **1. 교육 생성 API**

## ✔ url

**POST /edu**

---

## ✔ 설명

새로운 교육을 생성한다.

교육 생성 시 포함되는 주요 데이터:

- 교육 기본 정보(title, description)
- 교육 유형(category = 4대/직무/법정/기타 등, 4대/직무, MANDATORY, JOB, ETC)]
- 필수 여부(require)
- 이수 기준(passScore, passRatio)
- 수강 대상 부서(departmentScope)
- 초기 상태(created)

> 교육 자료, 스크립트, 영상은 별도 API로 추가 관리
>
> 본 API는 “교육 정보 자체”만 생성한다.

---

## ✔ 권한

`ROLE_ADMIN`

---

# 📌 **Request Body**

| key             | 설명                        | 타입          | Nullable | 예시                          |
| --------------- | --------------------------- | ------------- | -------- | ----------------------------- |
| title           | 교육 제목                   | string        | false    | `“산업안전 교육”`             |
| description     | 교육 설명                   | string        | true     | `"2025년 필수 산업안전 교육"` |
| category        | 교육 카테고리               | string        | false    | `"MANDATORY"`                 |
| require         | 필수 여부                   | boolean       | false    | `true`                        |
| passScore       | 통과 기준 점수              | number        | true     | `80`                          |
| passRatio       | 시청률 기준(%)              | number        | true     | `90`                          |
| departmentScope | 수강 대상 부서 (JSON array) | array(string) | true     | `["인사팀","개발팀"]`         |

### Example Request

```json
{
  "title": "산업안전 교육",
  "description": "2025년 산업안전 필수 교육입니다.",
  "category": "MANDATORY",
  "require": true,
  "passScore": 80,
  "passRatio": 90,
  "departmentScope": ["인사팀", "개발팀"]
}
```

---

# 📌 **Response**

| key    | 설명           | 타입         | 예시         |
| ------ | -------------- | ------------ | ------------ |
| eduId  | 생성된 교육 ID | string(uuid) | `"d129-..."` |
| status | 생성 상태      | string       | `"CREATED"`  |

### Example Response

```json
{
  "eduId": "uuid",
  "status": "CREATED"
}
```

---

# 📌 **Status Codes**

| status                        | 의미                            |
| ----------------------------- | ------------------------------- |
| **201 Created**               | 교육 생성 성공                  |
| **400 Bad Request**           | 필수 값 누락(title/category 등) |
| **401 Unauthorized**          | 인증 실패                       |
| **403 Forbidden**             | ROLE_ADMIN 아님                 |
| **409 Conflict**              | 동일 교육명 중복 (optional)     |
| **500 Internal Server Error** | 생성 실패                       |
